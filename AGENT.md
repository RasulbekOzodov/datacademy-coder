# Project instructions for DataCademy Coder

This file is added to the agent's system prompt when it runs in this directory.
Keep it short (under ~1500 tokens). Typical content:

## Project
- TypeScript (Node 22, ESM, NodeNext). Source in `src/`, tests in `tests/`.
- Build: `npm run build`. Tests: `npm test`. Typecheck: `npm run typecheck`.

## Rules
- Relative imports must end with `.js` (NodeNext).
- Do not add dependencies without asking.
- After editing TypeScript, run `npm run typecheck`.
